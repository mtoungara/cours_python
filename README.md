#Cours de Python 
# Nous allons voir comment déclarer des variables en Python
# Une variable est un espace mémoire qui permet de stocker une valeur
# Des boucles, des conditions, des fonctions, des classes, etc. peuvent utiliser ces variables
# Les tuples sont immuables = Non modifiables

# Les fonctions en Python 
# Doit être définie par le mot clé def et contenir plusieurs instructions return

# Une fonction en Python est executée lors de chaque appel.

# Un dictionnaire est un ensemble d'associations clé, valeurs séparées par deux points.